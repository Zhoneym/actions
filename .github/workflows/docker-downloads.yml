name: Save Docker Images

on:
  workflow_dispatch:

jobs:
  save-images:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Pull Docker images
        run: |
          docker pull zilliz/attu:v2.6 && docker pull milvusdb/milvus:v2.6.2
          docker pull minio/minio:RELEASE.2024-12-18T13-15-44Z && docker pull quay.io/coreos/etcd:v3.5.18
      - name: Save Docker images to tar
        run: |
          docker save quay.io/coreos/etcd:v3.5.18 minio/minio:RELEASE.2024-12-18T13-15-44Z milvusdb/milvus:v2.6.2 zilliz/attu:v2.6 -o cpu.tar
      - name: Upload tar as artifact
        uses: actions/upload-artifact@v4
        with:
          name: cpu.tar
          path: cpu.tar

